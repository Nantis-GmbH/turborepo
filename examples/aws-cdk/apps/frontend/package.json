{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --port 3001 --clearScreen false",
    "buildFrontend": "vite build",
    "lint": "eslint src/**/*.tsx --fix && tsc --noEmit",
    "copyBackendConfiguration" : "cp ../backend/aws-config.json .",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "files": ["dist"],
  "devDependencies": {
    "@types/react": "^17.0.0",
    "@types/react-dom": "^17.0.0",
    "@vitejs/plugin-react-refresh": "^1.3.6",
    "configuration": "*",
    "typescript": "^4.5.3",
    "vite": "^2.6.14",
    "eslint": "^8.5.0",
    "backend" : "*"
  }
}
